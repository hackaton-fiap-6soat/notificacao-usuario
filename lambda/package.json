{
    "name": "notification-lambda",
    "version": "1.0.0",
    "description": "AWS Lambda function for processing SQS messages and sending emails via SES",
    "main": "index.js",
    "scripts": {
        "package": "zip -r ../terraform/lambda_function_payload.zip ."
    },
    "dependencies": {
        "@sendgrid/mail": "^8.1.4",
        "aws-sdk": "^2.814.0"
    }
}
